[project]
name = "api-engineering-workspace"
version = "0"
description = "API engineering subject workspace — FastAPI capstones"
requires-python = ">=3.12, <3.14"

[tool.uv]
package = false

[tool.uv.workspace]
members = [
    "capstones/01_trades_ledger",
]
