# STRETCH GOAL — api + postgres. Fill in the TODOs.
services:
  db:
    image: postgres:17
    environment:
      POSTGRES_USER: ledger
      POSTGRES_PASSWORD: ledger
      POSTGRES_DB: ledger
    ports: ["5432:5432"]
    # TODO: healthcheck (pg_isready -U ledger) + a named volume for persistence.

  api:
    build: .
    env_file: .env
    ports: ["8000:8000"]
    depends_on:
      db:
        condition: service_healthy   # TODO: requires the db healthcheck above
    # TODO: on start, run `alembic upgrade head` then launch gunicorn (or rely on
    #       the Dockerfile CMD and run migrations as a separate one-shot service).
